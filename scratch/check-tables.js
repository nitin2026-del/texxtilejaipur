const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { const { data, error } = await supabase.from('newsletters').insert([{email: 'test@example.com'}]).select('*'); console.log('Newsletters Error:', error); console.log('Newsletters Data:', data); } run();
